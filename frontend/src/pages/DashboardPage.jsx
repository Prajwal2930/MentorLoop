import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import LoadingSpinner from '../components/LoadingSpinner';
import SectionCard from '../components/SectionCard';
import { getDashboard } from '../services/dashboardService';

const DashboardPage = () => {
  const [dashboard, setDashboard] = useState(null); const [error, setError] = useState('');
  useEffect(() => { getDashboard().then(setDashboard).catch((e) => setError(e.response?.data?.message || 'Unable to load dashboard.')); }, []);
  if (!dashboard) return error ? <p className="text-red-600">{error}</p> : <LoadingSpinner label="Loading your dashboard..." />;
  const { user, careerGoal, profileCompletion, skills, careerReadinessScore, recentActivityMessage, roadmap } = dashboard;
  return <div className="mx-auto max-w-6xl space-y-7"><section><p className="text-sm font-semibold text-indigo-600 dark:text-indigo-400">Your workspace</p><h1 className="mt-1 text-3xl font-bold">Welcome back, {user.fullName.split(' ')[0]}.</h1><p className="mt-2 text-slate-600 dark:text-slate-300">Keep building towards {careerGoal.targetRole}.</p></section><SectionCard className="border-indigo-200 bg-gradient-to-r from-indigo-50 to-violet-50 dark:border-indigo-500/30 dark:from-indigo-500/10 dark:to-violet-500/10"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-lg font-bold">Ready for an AI review?</h2><p className="mt-1 text-sm text-slate-600 dark:text-slate-300">Review code quality or turn a GitHub project into portfolio evidence.</p></div><div className="flex flex-wrap gap-2"><Link to="/analyze-code" className="rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-indigo-700">Analyze Code</Link><Link to="/github-review" className="rounded-lg border border-indigo-300 px-4 py-2.5 text-center text-sm font-semibold text-indigo-700 hover:bg-indigo-100 dark:border-indigo-400 dark:text-indigo-300 dark:hover:bg-indigo-500/10">GitHub Review</Link></div></div></SectionCard><section className="grid gap-4 sm:grid-cols-3"><SectionCard><p className="text-sm text-slate-500">Profile completion</p><p className="mt-2 text-3xl font-bold text-indigo-600">{profileCompletion.percentage}%</p><Link className="mt-3 inline-block text-sm font-semibold text-indigo-600" to={user.profileCompleted ? '/profile' : '/onboarding'}>{user.profileCompleted ? 'Edit profile' : 'Finish onboarding'}</Link></SectionCard><SectionCard><p className="text-sm text-slate-500">Career readiness</p><p className="mt-2 text-3xl font-bold text-violet-600">{careerReadinessScore}%</p><p className="mt-3 text-sm text-slate-500">Temporary score based on profile and skills.</p></SectionCard><SectionCard><p className="text-sm text-slate-500">Tracked skills</p><p className="mt-2 text-3xl font-bold text-cyan-600">{skills.length}</p><Link className="mt-3 inline-block text-sm font-semibold text-indigo-600" to="/skills">Manage skills</Link></SectionCard></section><SectionCard><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-semibold text-indigo-600 dark:text-indigo-400">Learning roadmap</p><h2 className="mt-1 text-lg font-bold">{roadmap?.currentWeek ? `Week ${roadmap.currentWeek.weekNumber}: ${roadmap.currentWeek.title}` : 'Create your personalized roadmap'}</h2><p className="mt-1 text-sm text-slate-600 dark:text-slate-300">Today’s goal: {roadmap?.todayGoal || 'Generate a roadmap from your profile and recent activity.'}</p></div><div className="text-left sm:text-right"><p className="text-2xl font-bold text-indigo-600">{roadmap?.progress ?? 0}%</p><Link to={roadmap ? `/roadmap/${roadmap._id}` : '/roadmap'} className="mt-2 inline-block text-sm font-semibold text-indigo-600">{roadmap ? 'Open roadmap' : 'Generate roadmap'}</Link></div></div>{roadmap && <div className="mt-4 h-2 overflow-hidden rounded-full bg-slate-100 dark:bg-slate-800"><div className="h-full rounded-full bg-indigo-600" style={{ width: `${roadmap.progress}%` }} /></div>}</SectionCard><section className="grid gap-6 lg:grid-cols-2"><SectionCard><h2 className="text-lg font-bold">Learning progress</h2><div className="mt-5 space-y-4">{skills.length ? skills.map((skill) => <div key={skill._id}><div className="mb-1 flex justify-between text-sm"><span>{skill.skillName}</span><span>{skill.level}%</span></div><div className="h-2 overflow-hidden rounded-full bg-slate-100 dark:bg-slate-800"><div className="h-full rounded-full bg-indigo-600" style={{ width: `${skill.level}%` }} /></div></div>) : <p className="text-sm text-slate-500">Add skills to see your progress.</p>}</div></SectionCard><SectionCard><h2 className="text-lg font-bold">Recent activity</h2><div className="mt-5 rounded-lg border border-dashed border-slate-300 p-8 text-center text-sm text-slate-500 dark:border-slate-700">{recentActivityMessage}</div></SectionCard></section></div>;
};
export default DashboardPage;
